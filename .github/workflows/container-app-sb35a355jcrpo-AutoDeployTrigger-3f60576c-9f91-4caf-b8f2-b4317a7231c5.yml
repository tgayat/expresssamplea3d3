name: Trigger auto deployment for container-app-sb35a355jcrpo

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-sb35a355jcrpo-AutoDeployTrigger-3f60576c-9f91-4caf-b8f2-b4317a7231c5.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPSB35A355JCRPO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPSB35A355JCRPO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPSB35A355JCRPO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPSB35A355JCRPO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPSB35A355JCRPO_REGISTRY_PASSWORD }}
          containerAppName: container-app-sb35a355jcrpo
          resourceGroup: expresssamplea3d3b088
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


